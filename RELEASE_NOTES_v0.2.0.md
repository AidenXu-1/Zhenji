# 帧集 0.2.0｜公开测试版

这一版集中打磨了采集卡、截图复用和录屏编辑体验，也重新整理了设置界面。

## 下载选择

- 推荐：`Zhenji-0.2.0-macOS-arm64.dmg`
- 备用：`Zhenji-0.2.0-macOS-arm64.zip`

两个安装包包含同一版本。支持 macOS 15+ 和 Apple Silicon Mac。

## 主要更新

- 采集卡加入更自然的扇形浏览、方向键和滚动反馈、清脆的棘轮音效及独立音量控制。
- 采集卡支持当前筛选结果的批量复制与拖出，补齐首次计数、设置入口、删除、清空和贴边入口体验。
- 采集卡只会由快捷键或真实文件拖拽晃动后投放唤出；普通复制不会打断当前工作。
- 修复采集卡 Quick Look 空格键在 Finder 等下层应用中的穿透、焦点丢失与首次退出异常。
- 修复截图取消后再次启动的残留状态与错误提示，并保留用户截图前已经显示的帧集界面。
- 录屏编辑器加入空格播放/暂停，并将自动缩放改为按真实交互分段：空闲时恢复全画面，临近操作之间平滑过渡。
- 设置按功能重新归类，采集板、快捷键、截图、录屏、外观与权限各自集中；外观增加更细腻的宣纸材质和明暗主题选择。

## 首次打开

此版本没有经过 Apple 公证。若 macOS 阻止打开，请进入“系统设置”→“隐私与安全性”，在“安全性”区域点击“仍要打开”并再次确认。

屏幕录制、麦克风和摄像头仍需按 macOS 正常权限流程分别授权。“仍要打开”不能代替隐私权限。

## 发布验证

- 自动化测试：1237 项执行，6 项按机器条件跳过，0 失败
- 从内部冻结提交 `9792b592e34f4efaadfee5171d4fe0c01514d3ec` 导出纯净源码后重新构建
- DMG 完整性、只读挂载与 `/Applications` 入口验证通过
- ZIP 洁净度验证通过，不含 `__MACOSX`、`._*` 或 `.DS_Store`
- ZIP 与 DMG 中的应用可执行文件 SHA-256 一致
- ZIP 与 DMG 中的应用均通过包体签名完整性验证
- 发行二进制已检查本机绝对路径、内部任务标记、调试覆盖率数据和常见凭据特征
- 应用标识：`com.zhaoji.zhenji`
- 应用版本：`0.2.0 (2)`
- 架构：`arm64`
- 应用可执行文件 SHA-256：`b6fd38d7755fe07e4391be38f3870a2054c1e9840149ac43dae866172f52cb71`

安装包 SHA-256 请见随附的 `SHA256SUMS.txt`。

当前版本采用临时签名，未使用 Developer ID，也未经过 Apple 公证。包体签名完整性通过不等于 Apple 信任或 Gatekeeper 放行。

已知边界请查看 [KNOWN_ISSUES.md](https://github.com/AidenXu-1/Zhenji/blob/main/KNOWN_ISSUES.md)。
